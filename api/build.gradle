apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    compileOnly project(':android_stub')
    implementation project(':core')
}

group = 'com.andy.navigator'
version = '1.0.0'